TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USER_IDS=

# Workspace root for shell sessions. Commands and paths are constrained within this root.
WORKSPACE_DIR=

# Projects root (where new projects are created)
PROJECTS_DIR=

# (AI chat removed) — no AI keys required

# Optional tuning
MAX_OUTPUT_CHARS=3500
COMMAND_TIMEOUT_SECONDS=60

# tmux bridge (term mode)
# Number of lines to capture from bottom of pane for diffing
TMUX_CAPTURE_LINES=1200
# Max seconds to wait for pane output to settle
TMUX_TIMEOUT_SECONDS=20

# Command to start your Codex/Claude terminal inside a new project session
# Example: "codex" or "claude code" or "source .venv/bin/activate && codex"
TMUX_CODEX_CMD=
